name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Unit tests
        run: node --experimental-strip-types --test $(find src -name '*.test.ts' | sort | tr '\n' ' ')

      - name: Migrate + seed build database
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/postgres?options=-c%20search_path%3Dpanel_club%2Cpublic
        run: |
          npm run db:migrate
          npm run db:seed

      - name: Next.js build
        # generateStaticParams + revalidate prerender routes at build time, so
        # this step needs the migrated, seeded Postgres service.
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/postgres?options=-c%20search_path%3Dpanel_club%2Cpublic
        run: npx next build
